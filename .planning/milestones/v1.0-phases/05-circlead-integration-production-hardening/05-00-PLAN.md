---
phase: 05-circlead-integration-production-hardening
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - fabric-app/pom.xml
  - fabric-app/src/main/resources/application.yml
  - fabric-app/src/main/resources/db/migration/V28__model_config_lifecycle.sql
  - fabric-core/src/test/resources/db/migration/V28__model_config_lifecycle.sql
  - fabric-projections/src/test/resources/db/migration/V28__model_config_lifecycle.sql
  - fabric-rules/src/test/resources/db/migration/V28__model_config_lifecycle.sql
  - fabric-app/src/test/java/dev/tessera/app/metrics/TesseraMetricsTest.java
  - fabric-app/src/test/java/dev/tessera/app/health/AgeGraphHealthIndicatorTest.java
  - fabric-app/src/test/java/dev/tessera/app/health/ConnectorHealthIndicatorTest.java
  - fabric-core/src/test/java/dev/tessera/core/events/EventRetentionJobTest.java
  - fabric-core/src/test/java/dev/tessera/core/events/snapshot/EventSnapshotServiceTest.java
  - fabric-connectors/src/test/java/dev/tessera/connectors/circlead/CircleadConnectorIT.java
autonomous: true
requirements: [OPS-01, OPS-02, OPS-03, OPS-04, CIRC-01]

must_haves:
  truths:
    - "V28 migration adds retention_days and snapshot_boundary columns to model_config"
    - "Prometheus and OTel dependencies are available on the classpath"
    - "Test stubs exist for all Wave 1-3 production code"
  artifacts:
    - path: "fabric-app/src/main/resources/db/migration/V28__model_config_lifecycle.sql"
      provides: "Lifecycle columns for event retention and snapshots"
      contains: "retention_days"
    - path: "fabric-app/pom.xml"
      provides: "Micrometer Prometheus and OTel dependencies"
      contains: "micrometer-registry-prometheus"
    - path: "fabric-app/src/test/java/dev/tessera/app/metrics/TesseraMetricsTest.java"
      provides: "Test stub for TesseraMetrics"
      contains: "class TesseraMetricsTest"
  key_links:
    - from: "V28 migration"
      to: "model_config table (V27)"
      via: "ALTER TABLE ADD COLUMN"
      pattern: "ALTER TABLE model_config"
---

<objective>
Wave 0: Foundation — V28 migration, Prometheus/OTel dependencies, application.yml updates, and test stubs for all Phase 5 production code.

Purpose: Every subsequent wave depends on the V28 migration (retention_days, snapshot_boundary columns) and the Prometheus dependency. Test stubs satisfy Nyquist compliance by ensuring every production class has a corresponding test file before implementation begins.
Output: V28 migration in all 4 migration directories, updated pom.xml and application.yml, 6 test stub files.
</objective>

<execution_context>
@/Users/matthiaswegner/.claude/get-shit-done/workflows/execute-plan.md
@/Users/matthiaswegner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-circlead-integration-production-hardening/05-CONTEXT.md
@.planning/phases/05-circlead-integration-production-hardening/05-PATTERNS.md

<interfaces>
<!-- Existing model_config table from V27 -->
From fabric-app/src/main/resources/db/migration/V27__tenant_hash_chain_config.sql:
Creates model_config table with (model_id UUID PK, hash_chain_enabled BOOLEAN NOT NULL DEFAULT false).
V28 adds retention_days INT NULL and snapshot_boundary TIMESTAMPTZ NULL.

<!-- Existing pom.xml structure -->
From fabric-app/pom.xml:
spring-boot-starter-actuator already present at line 47.
New dependencies go into same <dependencies> block, no version needed (BOM-managed).

<!-- application.yml management section (lines 74-82) -->
management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: when-authorized
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: V28 migration + POM dependencies + application.yml</name>
  <read_first>
    - fabric-app/pom.xml
    - fabric-app/src/main/resources/application.yml
    - fabric-app/src/main/resources/db/migration/V27__tenant_hash_chain_config.sql
  </read_first>
  <files>
    fabric-app/src/main/resources/db/migration/V28__model_config_lifecycle.sql,
    fabric-core/src/test/resources/db/migration/V28__model_config_lifecycle.sql,
    fabric-projections/src/test/resources/db/migration/V28__model_config_lifecycle.sql,
    fabric-rules/src/test/resources/db/migration/V28__model_config_lifecycle.sql,
    fabric-app/pom.xml,
    fabric-app/src/main/resources/application.yml
  </files>
  <action>
    1. Create V28__model_config_lifecycle.sql per Pattern 7 in 05-PATTERNS.md:
       ```sql
       -- Phase 5 / OPS-03 / OPS-04: event-log lifecycle columns for model_config
       ALTER TABLE model_config
           ADD COLUMN IF NOT EXISTS retention_days      INT         NULL,
           ADD COLUMN IF NOT EXISTS snapshot_boundary   TIMESTAMPTZ NULL;
       COMMENT ON COLUMN model_config.retention_days IS 'OPS-04: days to retain events; NULL means retain forever';
       COMMENT ON COLUMN model_config.snapshot_boundary IS 'OPS-03: earliest event_time still available via temporal replay; events below this were compacted';
       ```
    2. Copy the identical V28 file to all 4 migration directories:
       - fabric-app/src/main/resources/db/migration/
       - fabric-core/src/test/resources/db/migration/
       - fabric-projections/src/test/resources/db/migration/
       - fabric-rules/src/test/resources/db/migration/
    3. In fabric-app/pom.xml, add two dependencies after the spring-boot-starter-actuator entry (no version — BOM-managed):
       - io.micrometer:micrometer-registry-prometheus (per D-B1)
       - io.micrometer:micrometer-tracing-bridge-otel (per D-B3)
    4. In fabric-app/src/main/resources/application.yml, update the management section:
       - Change `include: health,info` to `include: health,info,prometheus`
       - Add `prometheus:` under `endpoint:` with `access: unrestricted` (per D-B1 — Prometheus scraper cannot send JWT)
  </action>
  <verify>
    <automated>cd /Users/matthiaswegner/Programmming/GitHub/Tessera && grep -q "retention_days" fabric-app/src/main/resources/db/migration/V28__model_config_lifecycle.sql && grep -q "micrometer-registry-prometheus" fabric-app/pom.xml && grep -q "prometheus" fabric-app/src/main/resources/application.yml && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - grep "retention_days" fabric-app/src/main/resources/db/migration/V28__model_config_lifecycle.sql returns match
    - grep "snapshot_boundary" fabric-app/src/main/resources/db/migration/V28__model_config_lifecycle.sql returns match
    - grep "micrometer-registry-prometheus" fabric-app/pom.xml returns match
    - grep "micrometer-tracing-bridge-otel" fabric-app/pom.xml returns match
    - grep "prometheus" fabric-app/src/main/resources/application.yml returns match
    - V28 file exists in all 4 migration directories (fabric-app, fabric-core test, fabric-projections test, fabric-rules test)
  </acceptance_criteria>
  <done>V28 migration deployed to all 4 directories, POM has Prometheus+OTel deps, application.yml exposes prometheus endpoint.</done>
</task>

<task type="auto">
  <name>Task 2: Test stubs for all Phase 5 production classes</name>
  <read_first>
    - fabric-connectors/src/test/java/dev/tessera/connectors/rest/RestPollingConnectorIT.java
    - fabric-core/src/test/java/dev/tessera/core/events/HashChainTest.java
  </read_first>
  <files>
    fabric-app/src/test/java/dev/tessera/app/metrics/TesseraMetricsTest.java,
    fabric-app/src/test/java/dev/tessera/app/health/AgeGraphHealthIndicatorTest.java,
    fabric-app/src/test/java/dev/tessera/app/health/ConnectorHealthIndicatorTest.java,
    fabric-core/src/test/java/dev/tessera/core/events/EventRetentionJobTest.java,
    fabric-core/src/test/java/dev/tessera/core/events/snapshot/EventSnapshotServiceTest.java,
    fabric-connectors/src/test/java/dev/tessera/connectors/circlead/CircleadConnectorIT.java
  </files>
  <action>
    Create 6 test stub files — each with a single @Disabled("Phase 5 Wave N — stub for Nyquist compliance") test method. Use JUnit 5 imports. Each stub should have:
    - Correct package declaration matching directory structure
    - @Disabled annotation with descriptive message
    - One @Test method named `placeholder()` that calls `fail("Not yet implemented")`

    Specific stubs:
    1. TesseraMetricsTest — `package dev.tessera.app.metrics;` — stub for OPS-01 metric registration tests
    2. AgeGraphHealthIndicatorTest — `package dev.tessera.app.health;` — stub for OPS-02 AGE health check
    3. ConnectorHealthIndicatorTest — `package dev.tessera.app.health;` — stub for OPS-02 connector health check
    4. EventRetentionJobTest — `package dev.tessera.core.events;` — stub for OPS-04 retention sweep tests
    5. EventSnapshotServiceTest — `package dev.tessera.core.events.snapshot;` — stub for OPS-03 snapshot compaction tests
    6. CircleadConnectorIT — `package dev.tessera.connectors.circlead;` — stub for CIRC-01 circlead connector integration test (WireMock-based, per D-A4)
  </action>
  <verify>
    <automated>cd /Users/matthiaswegner/Programmming/GitHub/Tessera && find . -name "TesseraMetricsTest.java" -o -name "AgeGraphHealthIndicatorTest.java" -o -name "ConnectorHealthIndicatorTest.java" -o -name "EventRetentionJobTest.java" -o -name "EventSnapshotServiceTest.java" -o -name "CircleadConnectorIT.java" | wc -l | grep -q "6" && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - grep "class TesseraMetricsTest" fabric-app/src/test/java/dev/tessera/app/metrics/TesseraMetricsTest.java returns match
    - grep "class AgeGraphHealthIndicatorTest" fabric-app/src/test/java/dev/tessera/app/health/AgeGraphHealthIndicatorTest.java returns match
    - grep "class ConnectorHealthIndicatorTest" fabric-app/src/test/java/dev/tessera/app/health/ConnectorHealthIndicatorTest.java returns match
    - grep "class EventRetentionJobTest" fabric-core/src/test/java/dev/tessera/core/events/EventRetentionJobTest.java returns match
    - grep "class EventSnapshotServiceTest" fabric-core/src/test/java/dev/tessera/core/events/snapshot/EventSnapshotServiceTest.java returns match
    - grep "class CircleadConnectorIT" fabric-connectors/src/test/java/dev/tessera/connectors/circlead/CircleadConnectorIT.java returns match
    - All 6 stubs contain @Disabled annotation
  </acceptance_criteria>
  <done>6 test stubs exist with @Disabled annotations, satisfying Nyquist compliance for all Phase 5 production classes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| prometheus-scraper→actuator | Unauthenticated Prometheus scraper hits /actuator/prometheus |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-00-01 | I (Information Disclosure) | /actuator/prometheus | mitigate | `prometheus.access: unrestricted` is deliberate — network-level firewall on IONOS VPS restricts scraper IP; no PII in metric names/values; health details remain `when-authorized` |
| T-05-00-02 | T (Tampering) | V28 migration | accept | ALTER TABLE ADD COLUMN IF NOT EXISTS is idempotent; no risk of data corruption on re-run |
</threat_model>

<verification>
1. V28 migration file exists in all 4 directories with identical content
2. `grep -c "micrometer" fabric-app/pom.xml` returns 2 (two new dependencies)
3. application.yml includes `prometheus` in management endpoint exposure
4. All 6 test stubs compile (verified by `./mvnw -B -ntp compile -pl fabric-app,fabric-core,fabric-connectors -DskipTests`)
</verification>

<success_criteria>
- V28 migration adds retention_days (INT NULL) and snapshot_boundary (TIMESTAMPTZ NULL) to model_config
- Prometheus and OTel bridge dependencies are in fabric-app/pom.xml
- /actuator/prometheus endpoint is exposed in application.yml
- 6 test stub files exist with @Disabled annotations
- Build compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/05-circlead-integration-production-hardening/05-00-SUMMARY.md`
</output>
