---
phase: 05-circlead-integration-production-hardening
plan: 01
type: execute
wave: 1
depends_on: [05-00]
files_modified:
  - fabric-app/src/main/java/dev/tessera/app/metrics/TesseraMetrics.java
  - fabric-app/src/main/java/dev/tessera/app/health/AgeGraphHealthIndicator.java
  - fabric-app/src/main/java/dev/tessera/app/health/ConnectorHealthIndicator.java
  - fabric-app/src/test/java/dev/tessera/app/metrics/TesseraMetricsTest.java
  - fabric-app/src/test/java/dev/tessera/app/health/AgeGraphHealthIndicatorTest.java
  - fabric-app/src/test/java/dev/tessera/app/health/ConnectorHealthIndicatorTest.java
autonomous: true
requirements: [OPS-01, OPS-02]

must_haves:
  truths:
    - "Prometheus scrape endpoint at /actuator/prometheus returns Tessera custom metrics"
    - "Health endpoint reports AGE graph status and connector status"
    - "Custom counters for ingest rate, rule evaluations, conflicts are registered"
    - "Gauges for outbox lag and replication slot lag poll DB on scrape"
    - "SHACL validation timer is registered"
  artifacts:
    - path: "fabric-app/src/main/java/dev/tessera/app/metrics/TesseraMetrics.java"
      provides: "Central metric registration bean"
      contains: "tessera.ingest.rate"
    - path: "fabric-app/src/main/java/dev/tessera/app/health/AgeGraphHealthIndicator.java"
      provides: "AGE graph health check"
      contains: "ag_catalog.ag_graph"
    - path: "fabric-app/src/main/java/dev/tessera/app/health/ConnectorHealthIndicator.java"
      provides: "Per-connector health status"
      contains: "connector_sync_status"
  key_links:
    - from: "TesseraMetrics"
      to: "MeterRegistry"
      via: "constructor injection"
      pattern: "MeterRegistry"
    - from: "AgeGraphHealthIndicator"
      to: "/actuator/health"
      via: "AbstractHealthIndicator auto-discovery"
      pattern: "@Component.*ageGraph"
---

<objective>
Observability: Prometheus metrics instrumentation and custom Actuator health indicators for AGE graph and connector status.

Purpose: Per D-B1 and D-B2, operators need Prometheus-scrapable metrics (ingest rate, rule evaluations, conflict count, outbox lag, replication slot lag, SHACL validation time) and health endpoints that report AGE, Postgres, Vault, and connector status. This plan delivers the metric beans and health indicators.
Output: TesseraMetrics.java, AgeGraphHealthIndicator.java, ConnectorHealthIndicator.java, plus enabled test files.
</objective>

<execution_context>
@/Users/matthiaswegner/.claude/get-shit-done/workflows/execute-plan.md
@/Users/matthiaswegner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-circlead-integration-production-hardening/05-CONTEXT.md
@.planning/phases/05-circlead-integration-production-hardening/05-PATTERNS.md
@.planning/phases/05-circlead-integration-production-hardening/05-00-SUMMARY.md

<interfaces>
<!-- WriteRateCircuitBreaker MeterRegistry pattern (analog for TesseraMetrics) -->
From fabric-rules/src/main/java/dev/tessera/rules/circuit/WriteRateCircuitBreaker.java:
- Injects MeterRegistry in constructor
- Null-guards before .register()
- Uses Counter.builder("tessera.circuit.tripped").description("...").tag("key","value").register(registry)

<!-- DebeziumSlotHealthIndicator (analog for health indicators) -->
From fabric-projections/src/main/java/dev/tessera/projections/kafka/DebeziumSlotHealthIndicator.java:
- Extends AbstractHealthIndicator
- @Component("debeziumSlot")
- @ConditionalOnProperty(name = "tessera.kafka.enabled", havingValue = "true")
- Injects NamedParameterJdbcTemplate
- doHealthCheck(Health.Builder builder) sets builder.up()/down() with withDetail()

<!-- ConnectorStatusController query pattern -->
From fabric-connectors/src/main/java/dev/tessera/connectors/admin/ConnectorStatusController.java:
- Queries connector_sync_status table
- JOIN connectors c ON c.id = css.connector_id
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TesseraMetrics — custom Micrometer counters, gauges, and timer</name>
  <read_first>
    - fabric-rules/src/main/java/dev/tessera/rules/circuit/WriteRateCircuitBreaker.java
    - fabric-app/src/test/java/dev/tessera/app/metrics/TesseraMetricsTest.java
    - fabric-app/src/main/resources/application.yml
  </read_first>
  <files>
    fabric-app/src/main/java/dev/tessera/app/metrics/TesseraMetrics.java,
    fabric-app/src/test/java/dev/tessera/app/metrics/TesseraMetricsTest.java
  </files>
  <behavior>
    - Test 1: After constructing TesseraMetrics with a SimpleMeterRegistry, registry.find("tessera.ingest.rate").counter() is not null
    - Test 2: After constructing TesseraMetrics, registry.find("tessera.rules.evaluations").counter() is not null
    - Test 3: After constructing TesseraMetrics, registry.find("tessera.conflicts.count").counter() is not null
    - Test 4: registry.find("tessera.outbox.lag").gauge() is not null (Gauge polling DB)
    - Test 5: registry.find("tessera.replication.slot.lag").gauge() is not null
    - Test 6: recordIngest() increments tessera.ingest.rate counter by 1
    - Test 7: recordRuleEvaluation() increments tessera.rules.evaluations counter by 1
    - Test 8: recordConflict() increments tessera.conflicts.count counter by 1
    - Test 9: shaclTimer() returns a Timer registered as "tessera.shacl.validation.time"
  </behavior>
  <action>
    Per D-B1, create `TesseraMetrics.java` in `fabric-app/src/main/java/dev/tessera/app/metrics/` following Pattern 1 from 05-PATTERNS.md. The class:
    - Is annotated @Component
    - Injects MeterRegistry and NamedParameterJdbcTemplate in constructor
    - Registers 3 Counters: `tessera.ingest.rate` (tag source=connector), `tessera.rules.evaluations`, `tessera.conflicts.count`
    - Registers 2 Gauges with DB-polling supplier lambdas: `tessera.outbox.lag` (SELECT COUNT(*) FROM graph_outbox WHERE status='PENDING'), `tessera.replication.slot.lag` (pg_wal_lsn_diff query on pg_replication_slots for slot 'tessera_outbox_slot'; returns -1 if no slot exists)
    - Registers 1 Timer: `tessera.shacl.validation.time` via a public `shaclTimer()` accessor returning a Timer
    - Public increment methods: recordIngest(), recordRuleEvaluation(), recordConflict()
    - Null-guard on NamedParameterJdbcTemplate in gauge lambdas (return 0 if null — graceful in unit tests without DB)

    Then enable TesseraMetricsTest (remove @Disabled) and implement the 9 test cases. For gauge tests, mock the NamedParameterJdbcTemplate or pass null and verify the gauge exists with a 0 or -1 value. Use SimpleMeterRegistry from Micrometer test support.
  </action>
  <verify>
    <automated>cd /Users/matthiaswegner/Programmming/GitHub/Tessera && ./mvnw -B -ntp test -pl fabric-app -Dtest=TesseraMetricsTest -Dsurefire.failIfNoSpecifiedTests=false</automated>
  </verify>
  <acceptance_criteria>
    - grep "tessera.ingest.rate" fabric-app/src/main/java/dev/tessera/app/metrics/TesseraMetrics.java returns match
    - grep "tessera.rules.evaluations" fabric-app/src/main/java/dev/tessera/app/metrics/TesseraMetrics.java returns match
    - grep "tessera.conflicts.count" fabric-app/src/main/java/dev/tessera/app/metrics/TesseraMetrics.java returns match
    - grep "tessera.outbox.lag" fabric-app/src/main/java/dev/tessera/app/metrics/TesseraMetrics.java returns match
    - grep "tessera.replication.slot.lag" fabric-app/src/main/java/dev/tessera/app/metrics/TesseraMetrics.java returns match
    - grep "tessera.shacl.validation.time" fabric-app/src/main/java/dev/tessera/app/metrics/TesseraMetrics.java returns match
    - grep -v "Disabled" fabric-app/src/test/java/dev/tessera/app/metrics/TesseraMetricsTest.java (no @Disabled remaining)
    - TesseraMetricsTest passes
  </acceptance_criteria>
  <done>6 Micrometer meters registered (3 counters, 2 gauges, 1 timer), public increment methods, all 9 tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AgeGraphHealthIndicator + ConnectorHealthIndicator</name>
  <read_first>
    - fabric-projections/src/main/java/dev/tessera/projections/kafka/DebeziumSlotHealthIndicator.java
    - fabric-app/src/test/java/dev/tessera/app/health/AgeGraphHealthIndicatorTest.java
    - fabric-app/src/test/java/dev/tessera/app/health/ConnectorHealthIndicatorTest.java
    - fabric-connectors/src/main/java/dev/tessera/connectors/admin/ConnectorStatusController.java
  </read_first>
  <files>
    fabric-app/src/main/java/dev/tessera/app/health/AgeGraphHealthIndicator.java,
    fabric-app/src/main/java/dev/tessera/app/health/ConnectorHealthIndicator.java,
    fabric-app/src/test/java/dev/tessera/app/health/AgeGraphHealthIndicatorTest.java,
    fabric-app/src/test/java/dev/tessera/app/health/ConnectorHealthIndicatorTest.java
  </files>
  <behavior>
    AgeGraphHealthIndicator:
    - Test 1: When ag_catalog.ag_graph query returns ["tessera_graph"], health is UP with detail graphs_count=1
    - Test 2: When ag_catalog.ag_graph query returns empty list, health is UP with detail graphs_count=0 (AGE loaded but no graphs)
    - Test 3: When ag_catalog.ag_graph query throws exception (AGE not loaded), health is DOWN

    ConnectorHealthIndicator:
    - Test 4: When no enabled connectors exist, health is UP with empty details
    - Test 5: When one connector has last_outcome=SUCCESS, health is UP with that connector in details
    - Test 6: When one connector has last_outcome=FAILED, health is DOWN with details showing FAILED
    - Test 7: Mixed SUCCESS and FAILED — health is DOWN (any failure = down)
  </behavior>
  <action>
    Per D-B2, create two health indicators following Pattern 2 from 05-PATTERNS.md:

    **AgeGraphHealthIndicator.java** in `fabric-app/src/main/java/dev/tessera/app/health/`:
    - @Component("ageGraph")
    - Extends AbstractHealthIndicator
    - Injects NamedParameterJdbcTemplate
    - doHealthCheck queries `SELECT name FROM ag_catalog.ag_graph` via jdbc.queryForList
    - Empty result = UP (AGE loaded, no graphs yet — not a failure)
    - Exception from query = DOWN (AGE extension not loaded)
    - builder.up().withDetail("graphs_count", graphs.size())

    **ConnectorHealthIndicator.java** in `fabric-app/src/main/java/dev/tessera/app/health/`:
    - @Component("connectors")
    - Extends AbstractHealthIndicator
    - Injects NamedParameterJdbcTemplate
    - doHealthCheck queries `SELECT c.id, css.last_outcome, css.last_poll_at FROM connectors c LEFT JOIN connector_sync_status css ON c.id = css.connector_id WHERE c.enabled = TRUE`
    - Any row with last_outcome='FAILED' → builder.down()
    - All SUCCESS or no rows → builder.up()
    - Each connector id + outcome in withDetails()

    Then enable both test stubs (remove @Disabled) and implement tests. Mock NamedParameterJdbcTemplate to return controlled results. Use `new Health.Builder()` to verify the doHealthCheck behavior via direct call (not Spring context).
  </action>
  <verify>
    <automated>cd /Users/matthiaswegner/Programmming/GitHub/Tessera && ./mvnw -B -ntp test -pl fabric-app -Dtest="AgeGraphHealthIndicatorTest,ConnectorHealthIndicatorTest" -Dsurefire.failIfNoSpecifiedTests=false</automated>
  </verify>
  <acceptance_criteria>
    - grep "@Component.*ageGraph" fabric-app/src/main/java/dev/tessera/app/health/AgeGraphHealthIndicator.java returns match
    - grep "ag_catalog.ag_graph" fabric-app/src/main/java/dev/tessera/app/health/AgeGraphHealthIndicator.java returns match
    - grep "@Component.*connectors" fabric-app/src/main/java/dev/tessera/app/health/ConnectorHealthIndicator.java returns match
    - grep "connector_sync_status" fabric-app/src/main/java/dev/tessera/app/health/ConnectorHealthIndicator.java returns match
    - AgeGraphHealthIndicatorTest passes (3 tests)
    - ConnectorHealthIndicatorTest passes (4 tests)
  </acceptance_criteria>
  <done>Two health indicators registered under "ageGraph" and "connectors" keys in /actuator/health, all 7 tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| /actuator/health | Health details shown only when-authorized (existing config) |
| /actuator/prometheus | Unrestricted access for scraper — no secrets in metric values |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-01 | I (Information Disclosure) | AgeGraphHealthIndicator | accept | Only exposes graph count (integer), no tenant data; protected by `show-details: when-authorized` |
| T-05-01-02 | I (Information Disclosure) | ConnectorHealthIndicator | mitigate | Exposes connector IDs and outcome status; protected by `show-details: when-authorized` — only authenticated operators see details |
| T-05-01-03 | I (Information Disclosure) | TesseraMetrics | accept | Metric names/values contain no PII; counter values are aggregate — no per-tenant breakdown in metric names (tenant tag is optional and scoped to authenticated operator queries) |
| T-05-01-04 | D (Denial of Service) | Gauge DB queries | mitigate | Gauge lambdas execute on Prometheus scrape interval (~15s); bounded by HikariCP pool; query is lightweight (COUNT on indexed table) |
</threat_model>

<verification>
1. `./mvnw -B -ntp test -pl fabric-app -Dtest="TesseraMetricsTest,AgeGraphHealthIndicatorTest,ConnectorHealthIndicatorTest"` — all tests pass
2. TesseraMetrics registers 6 meters (3 counters, 2 gauges, 1 timer)
3. Health indicators extend AbstractHealthIndicator with correct @Component names
</verification>

<success_criteria>
- TesseraMetrics registers tessera.ingest.rate, tessera.rules.evaluations, tessera.conflicts.count (counters), tessera.outbox.lag, tessera.replication.slot.lag (gauges), tessera.shacl.validation.time (timer)
- AgeGraphHealthIndicator reports UP/DOWN based on ag_catalog query
- ConnectorHealthIndicator reports UP/DOWN based on connector sync status
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/05-circlead-integration-production-hardening/05-01-SUMMARY.md`
</output>
