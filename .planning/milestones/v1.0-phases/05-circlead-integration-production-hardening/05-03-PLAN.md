---
phase: 05-circlead-integration-production-hardening
plan: 03
type: execute
wave: 2
depends_on: [05-00]
files_modified:
  - fabric-core/src/main/java/dev/tessera/core/events/EventRetentionJob.java
  - fabric-core/src/main/java/dev/tessera/core/events/snapshot/EventSnapshotService.java
  - fabric-core/src/main/java/dev/tessera/core/events/snapshot/SnapshotResult.java
  - fabric-core/src/main/java/dev/tessera/core/admin/EventLifecycleController.java
  - fabric-core/src/test/java/dev/tessera/core/events/EventRetentionJobTest.java
  - fabric-core/src/test/java/dev/tessera/core/events/snapshot/EventSnapshotServiceTest.java
autonomous: true
requirements: [OPS-03, OPS-04]

must_haves:
  truths:
    - "Per-tenant event-log retention deletes events older than retention_days daily"
    - "Retention sweep respects snapshot_boundary and never deletes snapshot events"
    - "POST /admin/events/snapshot triggers three-phase compaction that does not block the write path"
    - "Temporal queries above the snapshot boundary still work after compaction"
    - "GET /admin/events/retention returns current retention config for a tenant"
  artifacts:
    - path: "fabric-core/src/main/java/dev/tessera/core/events/EventRetentionJob.java"
      provides: "Scheduled daily retention sweep"
      contains: "@SchedulerLock"
    - path: "fabric-core/src/main/java/dev/tessera/core/events/snapshot/EventSnapshotService.java"
      provides: "Three-transaction snapshot compaction"
      contains: "REQUIRES_NEW"
    - path: "fabric-core/src/main/java/dev/tessera/core/admin/EventLifecycleController.java"
      provides: "Admin endpoints for snapshot and retention"
      contains: "/admin/events"
  key_links:
    - from: "EventRetentionJob"
      to: "model_config.retention_days"
      via: "SELECT WHERE retention_days IS NOT NULL"
      pattern: "retention_days IS NOT NULL"
    - from: "EventSnapshotService"
      to: "model_config.snapshot_boundary"
      via: "UPDATE model_config SET snapshot_boundary"
      pattern: "snapshot_boundary"
    - from: "EventLifecycleController"
      to: "EventSnapshotService"
      via: "compact(modelId)"
      pattern: "snapshotService.compact"
---

<objective>
Event-Log Lifecycle: Per-tenant retention sweep job and on-demand snapshot compaction with admin endpoints.

Purpose: Per D-C1, D-C2, D-C3 — long-lived tenants need configurable event-log retention (daily sweep deleting old events) and on-demand snapshot compaction (reducing event log to current-state snapshots). Both operations are idempotent and non-blocking (separate transactions from the write path).
Output: EventRetentionJob, EventSnapshotService, SnapshotResult, EventLifecycleController, enabled tests.
</objective>

<execution_context>
@/Users/matthiaswegner/.claude/get-shit-done/workflows/execute-plan.md
@/Users/matthiaswegner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-circlead-integration-production-hardening/05-CONTEXT.md
@.planning/phases/05-circlead-integration-production-hardening/05-PATTERNS.md
@.planning/phases/05-circlead-integration-production-hardening/05-00-SUMMARY.md

<interfaces>
<!-- OutboxPoller @Scheduled + @SchedulerLock pattern -->
From fabric-core/src/main/java/dev/tessera/core/events/OutboxPoller.java:
@Scheduled(fixedDelay = 500)
@SchedulerLock(name = "tessera-outbox-poller", lockAtMostFor = "PT14S", lockAtLeastFor = "PT5S")
@Transactional(propagation = Propagation.REQUIRES_NEW)
public void poll()

<!-- LockProviderConfig already provides ShedLock LockProvider bean -->
From fabric-core/src/main/java/dev/tessera/core/events/internal/LockProviderConfig.java:
@Bean public LockProvider lockProvider(DataSource dataSource)

<!-- EventLog.append() -->
From fabric-core/src/main/java/dev/tessera/core/events/EventLog.java:
public void append(TenantContext ctx, UUID nodeUuid, String typeSlug, String eventType, ...)
Runs inside caller's @Transactional boundary

<!-- McpAuditController (admin endpoint analog) -->
From fabric-projections/src/main/java/dev/tessera/projections/mcp/audit/McpAuditController.java:
@RestController @RequestMapping("/admin/mcp/audit")
Uses @AuthenticationPrincipal Jwt jwt for tenant matching
Returns ResponseEntity<Map<String, Object>>

<!-- model_config table (V27 + V28) -->
Columns: model_id UUID PK, hash_chain_enabled BOOLEAN, retention_days INT NULL, snapshot_boundary TIMESTAMPTZ NULL
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: EventRetentionJob — scheduled daily sweep</name>
  <read_first>
    - fabric-core/src/main/java/dev/tessera/core/events/OutboxPoller.java
    - fabric-core/src/main/java/dev/tessera/core/events/internal/PartitionMaintenanceTask.java
    - fabric-core/src/test/java/dev/tessera/core/events/EventRetentionJobTest.java
    - fabric-app/src/main/resources/db/migration/V28__model_config_lifecycle.sql
  </read_first>
  <files>
    fabric-core/src/main/java/dev/tessera/core/events/EventRetentionJob.java,
    fabric-core/src/test/java/dev/tessera/core/events/EventRetentionJobTest.java
  </files>
  <behavior>
    - Test 1: sweep() with no tenants having retention_days set deletes nothing
    - Test 2: sweep() with tenant having retention_days=30 deletes events older than 30 days
    - Test 3: sweep() respects snapshot_boundary — does not delete events at or below snapshot_boundary
    - Test 4: sweep() with multiple tenants processes each independently
    - Test 5: sweep() does not delete SNAPSHOT event_type events (only regular events)
  </behavior>
  <action>
    Create `EventRetentionJob.java` in `fabric-core/src/main/java/dev/tessera/core/events/` following Pattern 3 from 05-PATTERNS.md:

    - @Component
    - Injects NamedParameterJdbcTemplate
    - @Scheduled(cron = "0 0 2 * * *") — daily at 02:00
    - @SchedulerLock(name = "tessera-event-retention", lockAtMostFor = "PT55M") — lock releases on crash before next 02:00
    - @Transactional(propagation = Propagation.REQUIRES_NEW) — own transaction, not inherited
    - sweep() method:
      1. SELECT model_id, retention_days, snapshot_boundary FROM model_config WHERE retention_days IS NOT NULL
      2. For each tenant: DELETE FROM graph_events WHERE model_id = :model_id::uuid AND event_time < now() - :days * INTERVAL '1 day' AND event_type != 'SNAPSHOT' AND event_time > COALESCE(:snapshot_boundary, '-infinity'::timestamptz)
      3. Log deleted count per tenant via SLF4J

    The snapshot_boundary guard ensures: (a) events that are snapshot markers are never deleted, (b) events below the snapshot boundary (already compacted) are not re-deleted. The `event_type != 'SNAPSHOT'` clause protects the snapshot events themselves.

    Enable EventRetentionJobTest (remove @Disabled) and implement 5 tests. Mock NamedParameterJdbcTemplate: for Test 1, return empty list from model_config query. For Tests 2-5, return controlled config rows and verify the DELETE SQL parameters via ArgumentCaptor on jdbc.update().
  </action>
  <verify>
    <automated>cd /Users/matthiaswegner/Programmming/GitHub/Tessera && ./mvnw -B -ntp test -pl fabric-core -Dtest=EventRetentionJobTest -Dsurefire.failIfNoSpecifiedTests=false</automated>
  </verify>
  <acceptance_criteria>
    - grep "@SchedulerLock" fabric-core/src/main/java/dev/tessera/core/events/EventRetentionJob.java returns match
    - grep "tessera-event-retention" fabric-core/src/main/java/dev/tessera/core/events/EventRetentionJob.java returns match
    - grep "retention_days IS NOT NULL" fabric-core/src/main/java/dev/tessera/core/events/EventRetentionJob.java returns match
    - grep "SNAPSHOT" fabric-core/src/main/java/dev/tessera/core/events/EventRetentionJob.java returns match
    - grep "snapshot_boundary" fabric-core/src/main/java/dev/tessera/core/events/EventRetentionJob.java returns match
    - EventRetentionJobTest passes (5 tests)
  </acceptance_criteria>
  <done>EventRetentionJob runs daily at 02:00, deletes events older than retention_days per tenant, respects snapshot boundary, 5 tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: EventSnapshotService + SnapshotResult + EventLifecycleController</name>
  <read_first>
    - fabric-core/src/main/java/dev/tessera/core/events/EventLog.java
    - fabric-projections/src/main/java/dev/tessera/projections/mcp/audit/McpAuditController.java
    - fabric-core/src/test/java/dev/tessera/core/events/snapshot/EventSnapshotServiceTest.java
  </read_first>
  <files>
    fabric-core/src/main/java/dev/tessera/core/events/snapshot/SnapshotResult.java,
    fabric-core/src/main/java/dev/tessera/core/events/snapshot/EventSnapshotService.java,
    fabric-core/src/main/java/dev/tessera/core/admin/EventLifecycleController.java,
    fabric-core/src/test/java/dev/tessera/core/events/snapshot/EventSnapshotServiceTest.java
  </files>
  <behavior>
    EventSnapshotService:
    - Test 1: compact() on tenant with 3 entities writes 3 SNAPSHOT events and records boundary
    - Test 2: compact() sets model_config.snapshot_boundary to the compaction timestamp
    - Test 3: compact() deletes pre-boundary events (non-SNAPSHOT type) and returns correct SnapshotResult counts
    - Test 4: compact() on tenant with no events returns SnapshotResult(boundary, 0, 0)
    - Test 5: compact() is idempotent — running twice does not corrupt state

    EventLifecycleController:
    - Test 6: POST /admin/events/snapshot with valid model_id returns 200 with SnapshotResult JSON
    - Test 7: GET /admin/events/retention with valid model_id returns retention_days and snapshot_boundary
    - Test 8: POST /admin/events/snapshot with tenant mismatch returns 403
  </behavior>
  <action>
    **SnapshotResult.java** — simple record in `fabric-core/src/main/java/dev/tessera/core/events/snapshot/`:
    ```java
    public record SnapshotResult(Instant boundary, int eventsWritten, int eventsDeleted) {}
    ```

    **EventSnapshotService.java** in `fabric-core/src/main/java/dev/tessera/core/events/snapshot/` following Pattern 11 from 05-PATTERNS.md:
    - @Service
    - Injects NamedParameterJdbcTemplate and TransactionTemplate (or PlatformTransactionManager)
    - public SnapshotResult compact(UUID modelId) performs three-phase compaction per D-C2 and D-C3:
      Phase 1 (read TX): SELECT DISTINCT ON (node_uuid) node_uuid, type_slug, payload FROM graph_events WHERE model_id = :mid::uuid AND event_type != 'SNAPSHOT' ORDER BY node_uuid, sequence_nr DESC — gets latest state per entity
      Phase 2 (write TX): For each entity, INSERT into graph_events a SNAPSHOT event with event_type='SNAPSHOT', full payload. Then UPDATE model_config SET snapshot_boundary = :boundary WHERE model_id = :mid::uuid
      Phase 3 (delete TX): DELETE FROM graph_events WHERE model_id = :mid::uuid AND event_time < :boundary AND event_type != 'SNAPSHOT'
    - Each phase in its own transaction via TransactionTemplate.execute() — never one big transaction
    - Return SnapshotResult(boundary, entities.size(), deletedCount)

    **EventLifecycleController.java** in `fabric-core/src/main/java/dev/tessera/core/admin/` following Pattern 4 from 05-PATTERNS.md:
    - @RestController @RequestMapping("/admin/events")
    - Injects EventSnapshotService and NamedParameterJdbcTemplate
    - POST /snapshot with @RequestParam("model_id") UUID modelId and @AuthenticationPrincipal Jwt jwt:
      - Tenant-match guard (same pattern as McpAuditController): if jwt.getClaimAsString("tenant") != modelId → 403
      - Delegates to snapshotService.compact(modelId)
      - Returns 200 with Map containing model_id, snapshot_boundary, events_written, events_deleted
    - GET /retention with @RequestParam("model_id") UUID modelId and @AuthenticationPrincipal Jwt jwt:
      - Tenant-match guard
      - Query model_config for retention_days and snapshot_boundary
      - Returns 200 with current config values (null-safe)
    - PUT /retention with @RequestParam("model_id") UUID modelId, @RequestParam("retention_days") Integer days, @AuthenticationPrincipal Jwt jwt:
      - Tenant-match guard
      - UPDATE model_config SET retention_days = :days WHERE model_id = :mid::uuid
      - Returns 200 with updated config

    Enable EventSnapshotServiceTest (remove @Disabled) and implement tests 1-5 for EventSnapshotService (mock JDBC). For tests 6-8, add EventLifecycleControllerTest or extend EventSnapshotServiceTest with MockMvc tests for the controller endpoints.
  </action>
  <verify>
    <automated>cd /Users/matthiaswegner/Programmming/GitHub/Tessera && ./mvnw -B -ntp test -pl fabric-core -Dtest="EventSnapshotServiceTest" -Dsurefire.failIfNoSpecifiedTests=false</automated>
  </verify>
  <acceptance_criteria>
    - grep "class SnapshotResult" fabric-core/src/main/java/dev/tessera/core/events/snapshot/SnapshotResult.java returns match
    - grep "compact" fabric-core/src/main/java/dev/tessera/core/events/snapshot/EventSnapshotService.java returns match
    - grep "snapshot_boundary" fabric-core/src/main/java/dev/tessera/core/events/snapshot/EventSnapshotService.java returns match
    - grep "SNAPSHOT" fabric-core/src/main/java/dev/tessera/core/events/snapshot/EventSnapshotService.java returns match
    - grep "/admin/events" fabric-core/src/main/java/dev/tessera/core/admin/EventLifecycleController.java returns match
    - grep "isTenantMatch\|tenant" fabric-core/src/main/java/dev/tessera/core/admin/EventLifecycleController.java returns match
    - EventSnapshotServiceTest passes (5+ tests)
  </acceptance_criteria>
  <done>EventSnapshotService compacts event log in three non-blocking transactions, EventLifecycleController exposes snapshot/retention admin endpoints with tenant guard, all tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin-JWT→EventLifecycleController | Authenticated operator triggers snapshot/retention operations |
| EventRetentionJob→graph_events | Background job deletes tenant data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03-01 | E (Elevation of Privilege) | EventLifecycleController | mitigate | JWT tenant-match guard prevents operator A from snapshotting tenant B's data; same pattern as McpAuditController |
| T-05-03-02 | T (Tampering) | EventSnapshotService | mitigate | Snapshot events use event_type='SNAPSHOT' — retention job explicitly excludes SNAPSHOT events from deletion; boundary is recorded atomically in same TX as snapshot writes |
| T-05-03-03 | D (Denial of Service) | EventRetentionJob | mitigate | @SchedulerLock prevents duplicate runs; lockAtMostFor="PT55M" guarantees lock release before next daily run; delete batches per tenant (not one giant DELETE) |
| T-05-03-04 | I (Information Disclosure) | GET /admin/events/retention | mitigate | Returns only retention_days (integer) and snapshot_boundary (timestamp) — no entity data; protected by JWT tenant guard |
</threat_model>

<verification>
1. `./mvnw -B -ntp test -pl fabric-core -Dtest="EventRetentionJobTest,EventSnapshotServiceTest"` — all tests pass
2. EventRetentionJob has @Scheduled + @SchedulerLock + REQUIRES_NEW annotations
3. EventSnapshotService uses three separate transactions (not one big TX)
4. EventLifecycleController has JWT tenant-match guard on all endpoints
</verification>

<success_criteria>
- EventRetentionJob sweeps daily, deletes events per tenant retention_days, respects snapshot boundary
- EventSnapshotService compacts in three non-blocking transactions, writes SNAPSHOT events, records boundary
- EventLifecycleController provides POST /admin/events/snapshot, GET /admin/events/retention, PUT /admin/events/retention
- All endpoints enforce JWT tenant matching
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/05-circlead-integration-production-hardening/05-03-SUMMARY.md`
</output>
