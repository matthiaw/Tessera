---
phase: 04-sql-view-kafka-projections-hash-chained-audit
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - fabric-projections/src/test/java/dev/tessera/projections/sql/SqlViewProjectionIT.java
  - fabric-projections/src/test/java/dev/tessera/projections/sql/SqlViewSchemaChangeIT.java
  - fabric-core/src/test/java/dev/tessera/core/events/HashChainAppendIT.java
  - fabric-core/src/test/java/dev/tessera/core/audit/HashChainVerifyIT.java
  - fabric-app/src/test/java/dev/tessera/app/OutboxPollerConditionalIT.java
autonomous: true
requirements: [SQL-01, SQL-02, AUDIT-01, AUDIT-02, KAFKA-02]

must_haves:
  truths:
    - "All five test stubs compile and fail with meaningful assertion messages"
    - "Wave 1-2 plans can reference these test files in their verify commands"
    - "Each stub covers the core behavioral assertion for its requirement"
  artifacts:
    - path: "fabric-projections/src/test/java/dev/tessera/projections/sql/SqlViewProjectionIT.java"
      provides: "IT stub for SQL-01 view generation"
      contains: "@Testcontainers"
    - path: "fabric-projections/src/test/java/dev/tessera/projections/sql/SqlViewSchemaChangeIT.java"
      provides: "IT stub for SQL-02 schema-driven regeneration"
      contains: "@Testcontainers"
    - path: "fabric-core/src/test/java/dev/tessera/core/events/HashChainAppendIT.java"
      provides: "IT stub for AUDIT-01 hash chain append"
      contains: "@Testcontainers"
    - path: "fabric-core/src/test/java/dev/tessera/core/audit/HashChainVerifyIT.java"
      provides: "IT stub for AUDIT-02 verification endpoint"
      contains: "@Testcontainers"
    - path: "fabric-app/src/test/java/dev/tessera/app/OutboxPollerConditionalIT.java"
      provides: "IT stub for KAFKA-02 OutboxPoller conditionalization"
      contains: "@SpringBootTest"
  key_links: []
---

<objective>
Create test stubs (failing shells) for all Phase 4 integration tests so that Wave 1-2 plans have valid verify targets from the start. This is the Nyquist-compliance foundation — every plan's automated verify command must reference an existing test file.

Purpose: Plans 01-03 reference test files that do not yet exist. Without Wave 0, verify commands fail for the wrong reason (class not found vs assertion failure). Stubs establish the behavioral contract before implementation.

Output: 5 test stub files across fabric-projections, fabric-core, and fabric-app, each with @Disabled("Wave 0 stub — enabled by Plan 0X") annotations and skeleton test methods asserting the target behavior.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-sql-view-kafka-projections-hash-chained-audit/04-CONTEXT.md
@.planning/phases/04-sql-view-kafka-projections-hash-chained-audit/04-VALIDATION.md

<interfaces>
<!-- Test infrastructure patterns from existing codebase -->

From fabric-core AgePostgresContainer:
```java
public static PostgreSQLContainer<?> create() {
    DockerImageName image = DockerImageName.parse(AGE_IMAGE_DIGEST).asCompatibleSubstituteFor("postgres");
    return new PostgreSQLContainer<>(image)
            .withDatabaseName("tessera")
            .withUsername("tessera")
            .withPassword("tessera")
            .withReuse(true);
}
```

From fabric-core test patterns:
- `@Testcontainers` + `@Container static PostgreSQLContainer<?> pg = AgePostgresContainer.create()`
- `@DynamicPropertySource` to wire container to Spring context
- `@SpringBootTest` for full context tests
- `@TestPropertySource` for property overrides

From fabric-app existing IT patterns:
- `@SpringBootTest(webEnvironment = SpringBootTest.WebEnvironment.RANDOM_PORT)`
- `@TestPropertySource(properties = {"key=value"})`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 5 IT stub files for Phase 4 behavioral verification</name>
  <files>
    fabric-projections/src/test/java/dev/tessera/projections/sql/SqlViewProjectionIT.java,
    fabric-projections/src/test/java/dev/tessera/projections/sql/SqlViewSchemaChangeIT.java,
    fabric-core/src/test/java/dev/tessera/core/events/HashChainAppendIT.java,
    fabric-core/src/test/java/dev/tessera/core/audit/HashChainVerifyIT.java,
    fabric-app/src/test/java/dev/tessera/app/OutboxPollerConditionalIT.java
  </files>
  <read_first>
    fabric-core/src/test/java/dev/tessera/core/age/AgePostgresContainerIT.java,
    fabric-core/src/test/java/dev/tessera/core/events/EventLogIT.java,
    fabric-projections/src/test/java/dev/tessera/projections/mcp/audit/McpAuditControllerIT.java
  </read_first>
  <action>
Create 5 test stub files following the project's existing IT patterns. Each stub must:
1. Compile successfully
2. Be annotated with `@Disabled("Wave 0 stub — implementation in Plan 04-0X")`
3. Contain skeleton test methods with `fail("Not yet implemented")` bodies
4. Use the correct test infrastructure (@Testcontainers for DB tests, @SpringBootTest for context tests)

**SqlViewProjectionIT.java** (fabric-projections, @Testcontainers + AgePostgresContainer):
- `void viewCreatedForTenantNodeType()` — SQL-01: after creating node type, view is queryable
- `void viewColumnsMatchSchemaProperties()` — SQL-01: view columns match NodeTypeDescriptor
- `void viewExcludesTombstonedEntities()` — SQL-01: tombstoned nodes not in view
- `void viewDdlContainsSchemaVersionComment()` — D-D3: staleness detection

**SqlViewSchemaChangeIT.java** (fabric-projections, @Testcontainers + AgePostgresContainer):
- `void viewRegeneratedOnSchemaChange()` — SQL-03: schema change triggers view replacement
- `void viewsSurviveApplicationRestart()` — SQL-03: ApplicationRunner regenerates on startup

**HashChainAppendIT.java** (fabric-core, @Testcontainers + AgePostgresContainer):
- `void hashChainEnabledTenantHasPrevHash()` — AUDIT-01: enabled tenant events have prev_hash
- `void hashChainDisabledTenantHasNullPrevHash()` — AUDIT-01: disabled tenant has null
- `void concurrentAppendsProduceValidChain()` — AUDIT-01: 10 threads, valid chain

**HashChainVerifyIT.java** (fabric-core, @Testcontainers + AgePostgresContainer):
- `void validChainReturnsTrue()` — AUDIT-02: intact chain verifies
- `void tamperedChainReturnsBrokenAtSeq()` — AUDIT-02: tampered chain detected
- `void emptyTenantReturnsValid()` — AUDIT-02: no events = valid

**OutboxPollerConditionalIT.java** (fabric-app, @SpringBootTest):
- `void pollerExistsWhenKafkaDisabled()` — KAFKA-02: default = poller active
- `void pollerAbsentWhenKafkaEnabled()` — KAFKA-02: kafka enabled = poller disabled

Each file must be in the correct package directory and import the correct test infrastructure. Use the project's established `AgePostgresContainer.create()` pattern for DB-backed tests.
  </action>
  <verify>
    <automated>cd /Users/matthiaswegner/Programmming/GitHub/Tessera && mvn compile -pl fabric-core,fabric-projections,fabric-app -DskipTests -q 2>&1 | tail -10 && mvn test-compile -pl fabric-core,fabric-projections,fabric-app -DskipTests -q 2>&1 | tail -10</automated>
  </verify>
  <done>All 5 IT stub files exist, compile successfully, and are @Disabled. Plans 01-03 verify commands will find the test classes. Each stub documents the behavioral contract it will verify when enabled.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Test stubs only — no production code, no trust boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| N/A | N/A | N/A | N/A | Wave 0 contains only test stubs; no production code or security surface. |
</threat_model>

<verification>
1. `mvn test-compile -pl fabric-core,fabric-projections,fabric-app` — all stubs compile
2. `find . -name "*IT.java" -path "*/test/*"` confirms all 5 files exist
3. `grep -r "@Disabled" fabric-*/src/test/` shows all stubs are disabled
</verification>

<success_criteria>
- 5 test stub files exist in correct packages
- All stubs compile without errors
- All stubs are @Disabled and contain skeleton test methods
- Plans 01-03 verify commands can reference these test classes
</success_criteria>

<output>
After completion, create `.planning/phases/04-sql-view-kafka-projections-hash-chained-audit/04-00-SUMMARY.md`
</output>
