---
phase: 05-circlead-integration-production-hardening
plan: 02
type: execute
wave: 1
depends_on: [05-00]
files_modified:
  - fabric-connectors/src/main/java/dev/tessera/connectors/circlead/CircleadConnectorConfig.java
  - fabric-connectors/src/main/resources/connectors/circlead-role-mapping.json
  - fabric-connectors/src/main/resources/connectors/circlead-circle-mapping.json
  - fabric-connectors/src/main/resources/connectors/circlead-activity-mapping.json
  - fabric-connectors/src/test/java/dev/tessera/connectors/circlead/CircleadConnectorIT.java
  - docs/circlead-mapping.md
autonomous: true
requirements: [CIRC-01, CIRC-02, CIRC-03]

must_haves:
  truths:
    - "Tessera can poll circlead's REST API and ingest Role, Circle, and Activity data as graph nodes"
    - "The field-level mapping from circlead entities to Tessera node types is documented and implemented"
    - "circlead's graceful degradation when Tessera is unavailable is documented with circuit breaker configuration"
    - "Integration tests verify the connector works against a WireMock stub of circlead's API"
  artifacts:
    - path: "fabric-connectors/src/main/resources/connectors/circlead-role-mapping.json"
      provides: "Role mapping definition for GenericRestPollerConnector"
      contains: "circlead_id"
    - path: "fabric-connectors/src/main/resources/connectors/circlead-circle-mapping.json"
      provides: "Circle mapping definition"
      contains: "circlead_id"
    - path: "fabric-connectors/src/main/resources/connectors/circlead-activity-mapping.json"
      provides: "Activity mapping definition"
      contains: "circlead_id"
    - path: "docs/circlead-mapping.md"
      provides: "Documented entity mapping and circuit breaker config"
      contains: "Role"
    - path: "fabric-connectors/src/test/java/dev/tessera/connectors/circlead/CircleadConnectorIT.java"
      provides: "WireMock-based integration tests"
      contains: "WireMockExtension"
  key_links:
    - from: "CircleadConnectorConfig"
      to: "MappingDefinition"
      via: "classpath resource loading"
      pattern: "classpath:connectors/circlead"
    - from: "CircleadConnectorIT"
      to: "GenericRestPollerConnector"
      via: "poll() with MappingDefinition"
      pattern: "GenericRestPollerConnector"
---

<objective>
Circlead Integration: MappingDefinition JSONs for Role/Circle/Activity, connector configuration, WireMock integration tests, and documentation.

Purpose: Per D-A1, D-A2, D-A3, D-A4 — prove Tessera can ingest circlead data via the existing GenericRestPollerConnector, with documented field-level mappings and graceful degradation. No new connector code — reuses existing connector framework entirely.
Output: 3 mapping JSONs, CircleadConnectorConfig, CircleadConnectorIT (3+ tests), docs/circlead-mapping.md.
</objective>

<execution_context>
@/Users/matthiaswegner/.claude/get-shit-done/workflows/execute-plan.md
@/Users/matthiaswegner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-circlead-integration-production-hardening/05-CONTEXT.md
@.planning/phases/05-circlead-integration-production-hardening/05-PATTERNS.md
@.planning/phases/05-circlead-integration-production-hardening/05-00-SUMMARY.md

<interfaces>
<!-- MappingDefinition record -->
From fabric-connectors/src/main/java/dev/tessera/connectors/MappingDefinition.java:
public record MappingDefinition(
    String sourceEntityType,
    String targetNodeTypeSlug,
    String rootPath,
    List<FieldMapping> fields,
    List<String> identityFields,
    String sourceUrl,
    // Phase 2.5 fields (nullable):
    String syncStrategy, String syncInterval, String reconciliationChain,
    String extractionMode, String chunkStrategy, String extractionConfig
) {}

<!-- FieldMapping record -->
From fabric-connectors/src/main/java/dev/tessera/connectors/FieldMapping.java:
public record FieldMapping(String target, String source, String transform, boolean required) {}

<!-- GenericRestPollerConnector.poll() -->
From fabric-connectors/src/main/java/dev/tessera/connectors/rest/GenericRestPollerConnector.java:
public PollResult poll(Clock clock, MappingDefinition mapping, ConnectorState state, TenantContext tenant)

<!-- RestPollingConnectorIT (analog for CircleadConnectorIT) -->
From fabric-connectors/src/test/java/dev/tessera/connectors/rest/RestPollingConnectorIT.java:
Uses @RegisterExtension static WireMockExtension wm
Constructs MappingDefinition with all 12 fields (trailing nulls for Phase 2.5)
Asserts result.outcome() == SyncOutcome.SUCCESS and result.candidates() content
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Circlead mapping JSONs + CircleadConnectorConfig + CircleadConnectorIT</name>
  <read_first>
    - fabric-connectors/src/main/java/dev/tessera/connectors/MappingDefinition.java
    - fabric-connectors/src/main/java/dev/tessera/connectors/FieldMapping.java
    - fabric-connectors/src/main/java/dev/tessera/connectors/rest/GenericRestPollerConnector.java
    - fabric-connectors/src/test/java/dev/tessera/connectors/rest/RestPollingConnectorIT.java
    - fabric-connectors/src/test/java/dev/tessera/connectors/circlead/CircleadConnectorIT.java
  </read_first>
  <files>
    fabric-connectors/src/main/resources/connectors/circlead-role-mapping.json,
    fabric-connectors/src/main/resources/connectors/circlead-circle-mapping.json,
    fabric-connectors/src/main/resources/connectors/circlead-activity-mapping.json,
    fabric-connectors/src/main/java/dev/tessera/connectors/circlead/CircleadConnectorConfig.java,
    fabric-connectors/src/test/java/dev/tessera/connectors/circlead/CircleadConnectorIT.java
  </files>
  <action>
    **3 Mapping JSONs** per Pattern 5 from 05-PATTERNS.md in `fabric-connectors/src/main/resources/connectors/`:

    1. `circlead-role-mapping.json`:
       - sourceEntityType: "role", targetNodeTypeSlug: "role", rootPath: "$.content[*]"
       - sourceUrl: "${tessera.connectors.circlead.base-url}/circlead/workitem/list?type=ROLE&details=true"
       - identityFields: ["circlead_id"]
       - fields: id→circlead_id (required), title→title (required), abbreviation→abbreviation, purpose→purpose, status→status, type.name→role_type

    2. `circlead-circle-mapping.json`:
       - sourceEntityType: "circle", targetNodeTypeSlug: "circle", rootPath: "$.content[*]"
       - sourceUrl: "${tessera.connectors.circlead.base-url}/circlead/workitem/list?type=CIRCLE&details=true"
       - identityFields: ["circlead_id"]
       - fields: id→circlead_id (required), title→title (required), abbreviation→abbreviation, purpose→purpose, status→status

    3. `circlead-activity-mapping.json`:
       - sourceEntityType: "activity", targetNodeTypeSlug: "activity", rootPath: "$.content[*]"
       - sourceUrl: "${tessera.connectors.circlead.base-url}/circlead/workitem/list?type=ACTIVITY&details=true"
       - identityFields: ["circlead_id"]
       - fields: id→circlead_id (required), title→title (required), abbreviation→abbreviation, purpose→purpose, status→status

    **CircleadConnectorConfig.java** in `fabric-connectors/src/main/java/dev/tessera/connectors/circlead/`:
    - @Configuration
    - 3 @Bean methods, each loading a mapping JSON via @Value("classpath:connectors/circlead-{type}-mapping.json") Resource
    - Uses ObjectMapper.readValue(resource.getInputStream(), MappingDefinition.class) per Pattern 5
    - Bean names: circleadRoleMapping, circleadCircleMapping, circleadActivityMapping

    **CircleadConnectorIT.java** — enable (remove @Disabled) and implement per Pattern 6 from 05-PATTERNS.md:
    - @RegisterExtension static WireMockExtension wm
    - Test 1: `polls_role_list_and_maps_to_tessera_node` — stub GET /circlead/workitem/list?type=ROLE&details=true with a JSON response containing one role; assert PollResult has SyncOutcome.SUCCESS and one candidate with properties title="Product Owner", circlead_id="role-001"
    - Test 2: `polls_circle_list_and_maps_fields` — stub CIRCLE endpoint; assert candidate has title and circlead_id
    - Test 3: `polls_activity_list_and_maps_fields` — stub ACTIVITY endpoint; assert candidate has title and circlead_id
    - Test 4: `handles_empty_response_gracefully` — stub ROLE endpoint returning empty content array; assert PollResult has SUCCESS and 0 candidates

    Each test constructs MappingDefinition inline (like RestPollingConnectorIT) using wm.baseUrl() for the sourceUrl. Pass null for trailing Phase 2.5 fields. Use a mock or no-op GraphRepository (the connector's poll() method does not call graph directly).
  </action>
  <verify>
    <automated>cd /Users/matthiaswegner/Programmming/GitHub/Tessera && ./mvnw -B -ntp test -pl fabric-connectors -Dtest=CircleadConnectorIT -Dsurefire.failIfNoSpecifiedTests=false</automated>
  </verify>
  <acceptance_criteria>
    - grep "circlead_id" fabric-connectors/src/main/resources/connectors/circlead-role-mapping.json returns match
    - grep "circlead_id" fabric-connectors/src/main/resources/connectors/circlead-circle-mapping.json returns match
    - grep "circlead_id" fabric-connectors/src/main/resources/connectors/circlead-activity-mapping.json returns match
    - grep "class CircleadConnectorConfig" fabric-connectors/src/main/java/dev/tessera/connectors/circlead/CircleadConnectorConfig.java returns match
    - grep "WireMockExtension" fabric-connectors/src/test/java/dev/tessera/connectors/circlead/CircleadConnectorIT.java returns match
    - CircleadConnectorIT passes (4 tests)
  </acceptance_criteria>
  <done>3 mapping JSONs define circlead Role/Circle/Activity field mappings, CircleadConnectorConfig wires them as Spring beans, 4 integration tests prove the connector works against WireMock stubs.</done>
</task>

<task type="auto">
  <name>Task 2: circlead-mapping.md documentation (CIRC-02, CIRC-03)</name>
  <read_first>
    - fabric-connectors/src/main/resources/connectors/circlead-role-mapping.json
    - fabric-connectors/src/main/resources/connectors/circlead-circle-mapping.json
    - fabric-connectors/src/main/resources/connectors/circlead-activity-mapping.json
  </read_first>
  <files>docs/circlead-mapping.md</files>
  <action>
    Create `docs/circlead-mapping.md` with the following sections per D-A2 and D-A3:

    1. **Overview** — circlead reads from Tessera via REST and MCP projections in parallel with its own JPA model (per D-A1). A Tessera REST connector polls circlead's existing API to ingest data.

    2. **Entity Mapping Table** — for each circlead entity (Role, Circle, Activity):
       - Circlead entity class name (from circlead-sunray: WorkItem subtypes)
       - Tessera target node type slug
       - Field mapping table: circlead field path | Tessera property | Data type | Required | Notes
       - Identity field(s) used for delta detection
       - Edge types: Role→Circle (BELONGS_TO), Role→Activity (RESPONSIBLE_FOR), Circle→Circle (PARENT_OF) — these edge types should be documented as requiring Schema Registry configuration before connector activation

    3. **Circlead REST API Endpoints** — the three polling endpoints:
       - GET /circlead/workitem/list?type=ROLE&details=true
       - GET /circlead/workitem/list?type=CIRCLE&details=true
       - GET /circlead/workitem/list?type=ACTIVITY&details=true
       - Response format: RestEnvelope with content array

    4. **Connector Configuration** — how to register the circlead connector:
       - application.yml property: `tessera.connectors.circlead.base-url`
       - Polling interval (Claude's discretion: every 5 minutes)
       - Authentication: Bearer token if circlead requires it

    5. **Graceful Degradation (CIRC-03)** — document the circuit breaker pattern for circlead-side integration:
       - Recommend Spring Retry with @Retryable (maxAttempts=3, backoff 1s exponential) wrapping Tessera REST calls from within circlead's codebase
       - When retries exhausted, fall back to local JPA data
       - Sample @Retryable configuration snippet
       - Note: this is circlead-side code, not Tessera-side — Tessera's connector is the data ingestion direction

    6. **Round-trip Verification** — describe how to verify the mapping:
       - Ingest via connector → query via GET /api/v1/{model}/entities/role → verify fields match
       - MCP tool query: list_entity_types, get_entity for a known role → verify fields

    Use Apache 2.0 license header comment at top of file per project conventions.
  </action>
  <verify>
    <automated>cd /Users/matthiaswegner/Programmming/GitHub/Tessera && grep -q "Role" docs/circlead-mapping.md && grep -q "Circle" docs/circlead-mapping.md && grep -q "Activity" docs/circlead-mapping.md && grep -q "graceful" docs/circlead-mapping.md && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - grep "Role" docs/circlead-mapping.md returns match
    - grep "Circle" docs/circlead-mapping.md returns match
    - grep "Activity" docs/circlead-mapping.md returns match
    - grep "circlead_id" docs/circlead-mapping.md returns match
    - grep -i "graceful\|circuit.breaker\|degradation" docs/circlead-mapping.md returns match
    - grep -i "Retryable\|Spring Retry" docs/circlead-mapping.md returns match
  </acceptance_criteria>
  <done>circlead-mapping.md documents all field-level mappings for Role/Circle/Activity, edge types, connector configuration, graceful degradation with Spring Retry circuit breaker, and round-trip verification steps.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| circlead-API→Tessera-connector | External REST API polled by Tessera connector |
| Tessera-REST→circlead-consumer | circlead reads from Tessera's REST projection |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-01 | S (Spoofing) | circlead REST API | mitigate | Connector uses Bearer token from Vault for authentication to circlead API; token is never in config files (per SEC-02) |
| T-05-02-02 | T (Tampering) | MappingDefinition JSON | accept | Classpath resources are read-only at runtime; changes require rebuild+deploy |
| T-05-02-03 | I (Information Disclosure) | circlead data in Tessera | mitigate | All circlead data is tenant-scoped via model_id; cross-tenant isolation enforced by existing GraphService.apply() TenantContext mandatory parameter |
| T-05-02-04 | D (Denial of Service) | circlead API polling | mitigate | Polling interval is configurable (default 5min); existing ConnectorScheduler uses ShedLock to prevent duplicate polls; WriteRateCircuitBreaker protects against conflict storms |
</threat_model>

<verification>
1. `./mvnw -B -ntp test -pl fabric-connectors -Dtest=CircleadConnectorIT` — all 4 tests pass
2. docs/circlead-mapping.md covers all 3 entity types with field-level detail
3. 3 mapping JSON files are valid JSON and match MappingDefinition record structure
</verification>

<success_criteria>
- GenericRestPollerConnector successfully polls WireMock-stubbed circlead API and produces correct CandidateMutations
- 3 mapping JSONs cover Role, Circle, Activity with circlead_id as identity field
- circlead-mapping.md documents field mappings, edge types, connector config, and graceful degradation
- All integration tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/05-circlead-integration-production-hardening/05-02-SUMMARY.md`
</output>
