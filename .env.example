# Tessera local dev environment — copy to `.env` to override defaults.
#
# These values are LOCAL DEV ONLY. Production deploys (Phase 5) will source
# credentials from HashiCorp Vault (SEC-02). Do NOT commit a real `.env` file.

POSTGRES_USER=tessera
POSTGRES_PASSWORD=tessera
POSTGRES_DB=tessera
